import { Router } from 'express';
import {  } from '../controllers/PE_final_TDGE.controller.js';

const router = Router();
router.get('/PE_FINAL_TDGE',);
router.post('/PE_FINAL_TDGE',);
router.put('/PE_FINAL_TDGE/:id',);
router.delete('/PE_FINAL_TDGE/:id',);
router.get('/PE_FINAL_TDGE/:id',);

export default router;